# Budgeting
This project is aimed around helping the user understand their spending habits to accurately size budget categories.

# Good to know
- `consolidate.py` takes in unorganized data and spits out uniform data. 
- `categorize.py` takes in that uniform data and labels them with a category. 

## Must do's
- If a plan references a python package, check if it exists against what's already installed in the requirements.txt. If it's not, install it via pip. Make sure it is in installed against the virtual environment. Afterwards, update the requirements.txt

## DO NOT DO THESE
- Excessively add comments. This clutters code unnecessarily. Your job is to make the reviewer's life easier. Only add clarifiying comments if you must. 


